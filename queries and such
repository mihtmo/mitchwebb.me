CREATE TABLE observations (
    id int,
    time text,
    latitude float4,
    longitude float4,
    place text,
    inaturl text,
    photos text
);

INSERT INTO observations(id,
    time,
    latitude,
    longitude,
    place,
    inaturl,
    captive,
    photos)

SELECT


PATH="/Applications/Postgres.app/Contents/Versions/14/bin:$PATH"
heroku pg:psql -a mitchwebb postgresql-fluffy-62542
source "/Users/mitch/Desktop/My Computer/Coding Projects/Website/mitchwebb.me/.venv/bin/activate"
heroku logs --ps scheduler -a mitchwebb
HEROKU_POSTGRESQL_COPPER_URL=$(heroku config:get HEROKU_POSTGRESQL_COPPER_URL -a mitchwebb)

CREATE TABLE heatdata (
    date DATE
    temp_hi NUMERIC(3,1)
    temp_lo NUMERIC(3,1)
    visib NUMERIC(3,1)
    rain NUMERIC(3,2)
);
